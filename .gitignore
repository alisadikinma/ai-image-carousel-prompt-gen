# Local Claude Code settings (personal, not shared)
.claude/settings.local.json

# Local MCP configuration (development-only)
.mcp.json

# Plan artifacts (local working docs)
docs/plans/

# Junk artifacts (accidental shell expansion)
for
not
Other
*All

# Source files (raw PDFs/references, not for distribution)
sources/

# Compiled references (generated by scripts/compile-refs.ts, not committed —
# build artifact deployed to VPS at /home/claudesn/ via separate process)
references/compiled/

# Node / TypeScript
node_modules/
dist/
coverage/
*.log
npm-debug.log*

# Temp test output
tmp/

# OS
.DS_Store
Thumbs.db

# Editor
.idea/
.vscode/
*.swp
