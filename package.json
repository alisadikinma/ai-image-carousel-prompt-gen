{
  "name": "ai-image-carousel-prompt-gen",
  "version": "3.0.1",
  "description": "Cinematic AI image prompt generator for social media carousel content. Claude Code plugin emitting structured carousel JSON consumed by Portfolio_v2 backend's CarouselGenOutputAdapter.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "author": "Ali Sadikin <ali.sadikincom85@gmail.com>",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "compile-refs": "tsx scripts/compile-refs.ts"
  },
  "dependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0",
    "vitest": "^1.3.0"
  }
}
